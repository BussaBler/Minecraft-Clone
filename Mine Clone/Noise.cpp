#include "Noise.h"

Noise::Noise(float frequency, float amplitude, float offset, int maxHeight, float probability, BLOCKS block) : 
	frequency(frequency), amplitude(amplitude), offset(offset), maxHeight(maxHeight), probability(probability), block(block) {
	
}
